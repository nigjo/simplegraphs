A small collection of simple graphics only consists of straight lines.

 * Only `ctx.moveTo()` and `ctx.lineTo()` allowed
 * no random generator
 * no absolute values (all relative to width and height)
